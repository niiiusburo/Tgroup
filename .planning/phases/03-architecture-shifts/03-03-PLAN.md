---
phase: 03-architecture-shifts
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/routes/partners.js
  - website/src/lib/api.ts
  - website/src/pages/Customers.tsx
  - website/src/components/customer/CustomerProfile.tsx
autonomous: true
requirements:
  - REQ-06
must_haves:
  truths:
    - Soft-delete endpoint PATCH /api/Partners/:id/soft-delete sets isdeleted=true
    - Hard-delete endpoint DELETE /api/Partners/:id/hard-delete removes the row after FK-safe checks
    - Delete actions require customer:delete (soft) or customer:hard_delete (hard) permission
    - Delete confirmation dialog warns about linked appointments, saleorders, and dotkhams
    - Soft-deleted customers no longer appear in customer lists
  artifacts:
    - path: api/src/routes/partners.js
      provides: Two-tier delete API
      exports: ["PATCH /:id/soft-delete", "DELETE /:id/hard-delete"]
    - path: website/src/pages/Customers.tsx
      provides: List-level delete UI
      contains: customer:delete|customer:hard_delete|Xóa khách hàng
    - path: website/src/components/customer/CustomerProfile.tsx
      provides: Profile-level delete UI
      contains: customer:delete|customer:hard_delete|Xóa khách hàng
  key_links:
    - from: website/src/pages/Customers.tsx
      to: /api/Partners/:id/soft-delete
      via: softDeletePartner
      pattern: softDeletePartner|hardDeletePartner
    - from: api/src/routes/partners.js
      to: appointments|saleorders|dotkhams
      via: COUNT(*) checks before hard delete
      pattern: SELECT COUNT.*FROM appointments|saleorders|dotkhams
---

<objective>
Implement two-tier customer delete (soft + hard) with permission gating and FK-safe confirmation dialogs.

Purpose: Fulfill D-04, D-05, D-06 for safe customer removal.
Output: Backend delete endpoints and frontend delete buttons with warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-architecture-shifts/03-CONTEXT.md
@api/src/routes/partners.js
@website/src/pages/Customers.tsx
@website/src/components/customer/CustomerProfile.tsx
@website/src/lib/api.ts
@website/src/contexts/AuthContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add soft-delete and hard-delete endpoints to partners.js</name>
  <files>api/src/routes/partners.js</files>

  <read_first>
    - api/src/routes/partners.js
  </read_first>
  <action>
    Modify api/src/routes/partners.js:

    1. PATCH /api/Partners/:id/soft-delete
       - Verify partner exists and isdeleted = false.
       - Update: `UPDATE partners SET isdeleted = true, lastupdated = NOW() WHERE id = $1 AND customer = true AND isdeleted = false RETURNING *`
       - Return 200 with the updated row or 404 if not found.

    2. DELETE /api/Partners/:id/hard-delete
       - Verify partner exists and isdeleted = false (or allow regardless? Use false to be safe — but if already soft-deleted, admin may still want to purge. Allow both for admin power).
       - Run FK-safe checks with parameterized queries:
         - `SELECT COUNT(*) AS count FROM appointments WHERE partnerid = $1`
         - `SELECT COUNT(*) AS count FROM saleorders WHERE partnerid = $1 AND isdeleted = false`
         - `SELECT COUNT(*) AS count FROM dotkhams WHERE partnerid = $1 AND isdeleted = false`
       - If any count > 0, return 409 Conflict with JSON: `{ error: 'Partner has linked records', linked: { appointments: N, saleorders: N, dotkhams: N } }`
       - If safe, `DELETE FROM partners WHERE id = $1 AND customer = true RETURNING id`
       - Return 200 with `{ success: true, id }` or 404.

    Keep error handling consistent with existing routes (500 on unexpected error).
  </action>
  <verify>
    <automated>grep -n "soft-delete\|hard-delete\|linked records" api/src/routes/partners.js</automated>
  </verify>
  <acceptance_criteria>
    - PATCH /:id/soft-delete route exists and sets isdeleted = true
    - DELETE /:id/hard-delete route exists and checks linked records
    - Hard delete returns 409 with counts when linked records exist
    - Hard delete returns 200 when no linked records exist
    - Soft-deleted partners are excluded by existing GET / filters (already `isdeleted = false`)
  </acceptance_criteria>
  <done>Backend supports transactional soft delete and FK-safe hard delete</done>
</task>

<task type="auto">
  <name>Task 2: Add delete API wrappers in lib/api.ts</name>
  <files>website/src/lib/api.ts</files>

  <read_first>
    - website/src/lib/api.ts
  </read_first>
  <action>
    In website/src/lib/api.ts, after `updatePartner`, add:

    ```typescript
    export function softDeletePartner(id: string) {
      return apiFetch<ApiPartner>(`/Partners/${id}/soft-delete`, { method: 'PATCH' });
    }

    export function hardDeletePartner(id: string) {
      return apiFetch<{ success: boolean; id: string }>(`/Partners/${id}/hard-delete`, { method: 'DELETE' });
    }
    ```
  </action>
  <verify>
    <automated>grep -n "softDeletePartner\|hardDeletePartner" website/src/lib/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `softDeletePartner` function exported from lib/api.ts
    - `hardDeletePartner` function exported from lib/api.ts
    - Both use correct HTTP methods (PATCH and DELETE)
  </acceptance_criteria>
  <done>API client exposes soft and hard delete methods</done>
</task>

<task type="auto">
  <name>Task 3: Wire delete UI into Customers list and CustomerProfile</name>
  <files>website/src/pages/Customers.tsx, website/src/components/customer/CustomerProfile.tsx, website/src/lib/api.ts</files>

  <read_first>
    - website/src/pages/Customers.tsx
    - website/src/components/customer/CustomerProfile.tsx
    - website/src/contexts/AuthContext.tsx
  </read_first>
  <action>
    1. In website/src/pages/Customers.tsx:
       - Add permission checks:
         ```typescript
         const canSoftDelete = hasPermission('customer:delete');
         const canHardDelete = hasPermission('customer:hard_delete');
         ```
       - Add a delete handler that opens a confirmation dialog. Use a new local state:
         ```typescript
         const [deleteDialog, setDeleteDialog] = useState<{ open: boolean; customerId: string | null; customerName: string; mode: 'soft' | 'hard' } | null>(null);
         ```
       - In the DataTable, we can't easily add per-row actions without modifying DataTable. Better: add a small "Actions" render in `buildCustomerColumns` or add action buttons in the profile view only. Per D-04/D-05, delete should be accessible from the customer list or profile. Let's add it to the profile view which already has Edit, and also add a small row action using DataTable's onRowClick pattern? Actually DataTable doesn't support action columns out of the box.
       - **Decision:** Add the delete action to `CustomerProfile` (primary) and also add a delete button next to each row using an actions column in `buildCustomerColumns`. Wait — `buildCustomerColumns` is inside `Customers.tsx` and receives no extra props. We can add an `actions` parameter or inline the column definition.
       - Simpler: keep it in `CustomerProfile` only for now, which is the detailed view where admins review linked records before deleting. If the user later wants list-level delete, it can be added.
       - Actually D-04 says "in CustomerProfile / customer list". So we should add it to both.
       - For the list: modify `buildCustomerColumns` to accept an `onDelete` callback and render a small trash icon for soft-delete (if user has `customer:delete`). Hard delete is too dangerous for list view — keep it in profile only.
       - Re-implement columns inside the component (instead of memoized `buildCustomerColumns`) or pass `canSoftDelete` into `buildCustomerColumns`. Choose minimal change: pass `canSoftDelete` and `onSoftDelete` into `buildCustomerColumns`.

    2. In website/src/components/customer/CustomerProfile.tsx:
       - Add props: `onSoftDelete?: () => void; onHardDelete?: () => void; canSoftDelete?: boolean; canHardDelete?: boolean;`
       - In the header next to Edit, add a "Delete" dropdown or button group:
         - Primary red button "Xóa" with a small dropdown arrow.
         - Dropdown items: "Xóa mềm" (Soft delete) and "Xóa vĩnh viễn" (Hard delete — admin only).
       - When clicked, call the respective `onSoftDelete` / `onHardDelete` prop, which is handled by the parent.

    3. Back in website/src/pages/Customers.tsx:
       - Implement a shared `DeleteConfirmDialog` inline or as a small component in the same file (to avoid new files). The dialog should:
         - Show title: "Xác nhận xóa khách hàng"
         - Show message: `Bạn có chắc muốn xóa {name}?`
         - For hard delete, show an extra warning block in amber/red: `Thao tác này không thể hoàn tác. Khách hàng có {appointments} lịch hẹn, {saleorders} hóa đơn, {dotkhams} đợt khám liên quan.`
         - Buttons: "Hủy" (gray) and "Xóa" (red).
       - Fetch linked counts before showing hard-delete dialog:
         Use the existing `hookProfile` which already has `appointmentcount`, `ordercount`, `dotkhamcount` from the API.
       - On confirm soft delete:
         - Call `softDeletePartner(selectedCustomerId)`
         - Close profile, navigate to `/customers`, refetch list.
       - On confirm hard delete:
         - Call `hardDeletePartner(selectedCustomerId)`
         - If API returns 409, show error in dialog: `Không thể xóa: còn dữ liệu liên quet.`
         - On success, close dialog, navigate to `/customers`, refetch list.
  </action>
  <verify>
    <automated>grep -n "customer:delete\|customer:hard_delete\|softDeletePartner\|hardDeletePartner\|Xác nhận xóa" website/src/pages/Customers.tsx website/src/components/customer/CustomerProfile.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Customers.tsx checks `hasPermission('customer:delete')` and `hasPermission('customer:hard_delete')`
    - CustomerProfile.tsx renders a Delete button/dropdown when permissions allow
    - A confirmation dialog asks before deleting and shows linked record counts for hard delete
    - Soft delete calls `softDeletePartner` and navigates back to list on success
    - Hard delete calls `hardDeletePartner`, handles 409 error, and navigates back on success
  </acceptance_criteria>
  <done>Delete UI with permission gating and confirmation dialogs is wired across list and profile</done>
</task>

</tasks>

<verification>
- PATCH soft-delete sets isdeleted=true and customer disappears from list
- DELETE hard-delete returns 409 when linked records exist
- DELETE hard-delete succeeds when no linked records exist
- Frontend shows delete buttons only for users with correct permissions
- Confirmation dialog warns about linked appointments/saleorders/dotkhams
</verification>

<success_criteria>
- Two-tier delete API is live
- Frontend enforces permission checks per D-04
- Dialog warnings display linked record counts per D-05
- Existing GET routes already exclude isdeleted=true per D-06
</success_criteria>

<output>
After completion, create `.planning/phases/03-architecture-shifts/03-03-SUMMARY.md`
</output>
