---
phase: 03-architecture-shifts
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - website/src/lib/api.ts
  - website/src/types/employee.ts
  - website/src/hooks/useEmployees.ts
  - website/src/components/employees/EmployeeForm.tsx
  - website/src/components/employees/EmployeeTable.tsx
  - website/src/components/employees/EmployeeProfile.tsx
autonomous: true
requirements:
  - REQ-12
must_haves:
  truths:
    - EmployeeForm shows a primary LocationSelector and an additional branches multi-select
    - EmployeeTable renders all assigned branch names per employee
    - EmployeeProfile displays all assigned branches
    - Creating or editing an employee saves additional branch selections
  artifacts:
    - path: website/src/components/employees/EmployeeForm.tsx
      provides: Dual-selector branch UI
      contains: LocationSelector|Chi nhánh phụ|locationScopeIds
    - path: website/src/components/employees/EmployeeTable.tsx
      provides: Multi-branch display
      contains: locationName|locationScopeNames
    - path: website/src/components/employees/EmployeeProfile.tsx
      provides: Profile branch list
      contains: locationId|locationScopeIds
    - path: website/src/lib/api.ts
      provides: Typed scope fields
      contains: locationScopeIds
  key_links:
    - from: EmployeeForm.tsx
      to: /api/Employees
      via: createEmployee / updateEmployee
      pattern: locationScopeIds.*api
    - from: EmployeeTable.tsx
      to: useEmployees
      via: locationScopeNames
      pattern: locationScopeNames|locationName
---

<objective>
Build the frontend dual-selector multi-branch assignment UI for employees.

Purpose: Fulfill D-01 and D-02 so staff can be assigned to primary and additional branches.
Output: EmployeeForm with branch multi-select, EmployeeTable/Profile showing all branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-architecture-shifts/03-CONTEXT.md
@website/src/components/employees/EmployeeForm.tsx
@website/src/components/employees/EmployeeTable.tsx
@website/src/components/employees/EmployeeProfile.tsx
@website/src/hooks/useEmployees.ts
@website/src/lib/api.ts
@website/src/types/employee.ts
@website/src/components/employees/RoleMultiSelect.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add locationScopeIds to types and API client</name>
  <files>website/src/types/employee.ts, website/src/lib/api.ts, website/src/hooks/useEmployees.ts</files>

  <read_first>
    - website/src/types/employee.ts
    - website/src/lib/api.ts
    - website/src/hooks/useEmployees.ts
  </read_first>
  <action>
    1. In website/src/types/employee.ts:
       - Add `locationScopeIds?: readonly string[];` to the `Employee` interface.

    2. In website/src/lib/api.ts:
       - Add `locationScopeIds?: string[];` to `CreateEmployeeData` interface.
       - Add `locationScopeIds?: string[];` to `ApiEmployee` interface.
       - Ensure `createEmployee` and `updateEmployee` pass this field through (they already pass the whole data object).

    3. In website/src/hooks/useEmployees.ts:
       - Extend `EmployeeWithApiFields` to include `locationScopeIds?: readonly string[];`.
       - In `mapApiEmployeeToEmployee`, map `apiEmployee.locationScopeIds ?? []`.
       - When calling `fetchEmployees`, the API now returns `locationScopeIds` per 03-01.
  </action>
  <verify>
    <automated>grep -n "locationScopeIds" website/src/types/employee.ts website/src/lib/api.ts website/src/hooks/useEmployees.ts</automated>
  </verify>
  <acceptance_criteria>
    - `Employee` interface in types/employee.ts contains `locationScopeIds?: readonly string[]`
    - `CreateEmployeeData` and `ApiEmployee` in lib/api.ts contain `locationScopeIds?: string[]`
    - `mapApiEmployeeToEmployee` maps `locationScopeIds`
    - `npm run build` in website/ passes TypeScript compilation (or at least `npx tsc --noEmit` runs without new errors)
  </acceptance_criteria>
  <done>Types and hooks support locationScopeIds with TypeScript safety</done>
</task>

<task type="auto">
  <name>Task 2: Add multi-branch selector to EmployeeForm</name>
  <files>website/src/components/employees/EmployeeForm.tsx</files>

  <read_first>
    - website/src/components/employees/EmployeeForm.tsx
    - website/src/components/employees/RoleMultiSelect.tsx
  </read_first>
  <action>
    Modify website/src/components/employees/EmployeeForm.tsx:

    1. Extend props: `employee?.locationScopeIds` is now available from the type changes.

    2. Add state: `const [locationScopeIds, setLocationScopeIds] = useState<string[]>(employee?.locationScopeIds ?? []);`

    3. In the form body, after the existing "Chi nhánh" (primary branch) LocationSelector block, add a new section:
       - Label: "Chi nhánh phụ" with `Building2` icon (import from lucide-react).
       - Render all `locations` as toggleable chips (same pattern as RoleMultiSelect). Each chip shows the location name.
       - Clicking a chip toggles its selection in `locationScopeIds`.
       - Visually distinguish selected chips with `bg-orange-100 text-orange-700 border-orange-200` vs unselected `bg-gray-100 text-gray-600 hover:bg-gray-200`.
       - Exclude the primary `companyid` from the chip list (or show it disabled) to avoid duplicate selection.
       - If `locations` is empty, show a small gray text: "Chưa có chi nhánh nào".

    4. In `handleSubmit`, include `locationScopeIds` in the `data` payload sent to `createEmployee` / `updateEmployee`.

    Keep the existing modal styling, label font (`text-xs font-semibold text-gray-500 uppercase tracking-wider`), and spacing.
  </action>
  <verify>
    <automated>grep -n "Chi nhánh phụ\|locationScopeIds" website/src/components/employees/EmployeeForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - EmployeeForm.tsx contains "Chi nhánh phụ" label
    - State `locationScopeIds` is initialized from `employee?.locationScopeIds`
    - Form renders location chips that toggle selection
    - Selected chips use `bg-orange-100 text-orange-700 border-orange-200`
    - `handleSubmit` passes `locationScopeIds` in the data object
  </acceptance_criteria>
  <done>EmployeeForm supports selecting additional branches with chip toggles</done>
</task>

<task type="auto">
  <name>Task 3: Display all assigned branches in EmployeeTable and EmployeeProfile</name>
  <files>website/src/components/employees/EmployeeTable.tsx, website/src/components/employees/EmployeeProfile.tsx, website/src/pages/Employees/index.tsx, website/src/hooks/useEmployees.ts</files>

  <read_first>
    - website/src/components/employees/EmployeeTable.tsx
    - website/src/components/employees/EmployeeProfile.tsx
    - website/src/hooks/useEmployees.ts
  </read_first>
  <action>
    1. In website/src/hooks/useEmployees.ts:
       - After fetching employees, build a set of all unique scope company IDs.
       - Fetch company names for scope IDs using `fetchCompanies()` (already imported or available via lib/api). Alternatively, `ApiEmployee` currently has `companyname` only for primary branch. Since we need names for scope IDs too:
         After `fetchAndSetEmployees`, optionally fetch all companies once and attach `locationScopeNames` to each employee in the mapper. Better: since `locations` may not be in the hook, just add `locationScopeNames?: string[]` to the type and resolve in the table/profile via a locations map.
       - Simpler path: keep `locationScopeIds` on the Employee type. In EmployeeTable and EmployeeProfile, accept an optional `locationNameMap: Map<string, string>` prop or compute inline.

    2. In website/src/components/employees/EmployeeTable.tsx:
       - Change the `locationId` column render to show a comma-separated list of all branch names.
       - Since `EmployeeTable` doesn't currently have access to `allLocations`, add an optional prop `locationNameMap?: Map<string, string>`.
       - In the render, compute:
         ```
         const branchNames = [emp.locationName || emp.locationId, ...emp.locationScopeIds.map(id => locationNameMap?.get(id) || id)];
         ```
         Show them as small inline badges or comma-separated text with MapPin icon.
       - Update `Employees/index.tsx` to pass `locationNameMap` (it already has `allLocations` via `useLocations`).

    3. In website/src/components/employees/EmployeeProfile.tsx:
       - Under Contact info / MapPin, show the primary branch name as now.
       - Add a new line below it: "Chi nhánh phụ:" followed by comma-separated names of additional branches (resolved from `locationScopeIds` using the locations fetch already in the component).
       - If no additional branches, show "Không có chi nhánh phụ".

    4. In website/src/pages/Employees/index.tsx:
       - Import `useLocations` if not already imported.
       - Build `locationNameMap` from `allLocations`.
       - Pass `locationNameMap={locationNameMap}` to `<EmployeeTable />`.
  </action>
  <verify>
    <automated>grep -n "locationScopeIds\|locationNameMap\|Chi nhánh phụ" website/src/components/employees/EmployeeTable.tsx website/src/components/employees/EmployeeProfile.tsx website/src/pages/Employees/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - EmployeeTable.tsx uses `locationScopeIds` to render branch names
    - Employees/index.tsx passes `locationNameMap` to EmployeeTable
    - EmployeeProfile.tsx shows primary branch and additional branches separately
    - If no additional branches, profile shows "Không có chi nhánh phụ"
  </acceptance_criteria>
  <done>EmployeeTable and EmployeeProfile display all assigned branch names</done>
</task>

</tasks>

<verification>
- TypeScript builds without errors in website/
- EmployeeForm renders primary selector + additional branch chips
- EmployeeTable shows multi-branch names
- EmployeeProfile shows primary branch and additional branches
- Saving an employee persists additional branches (verified by API response and UI re-render)
</verification>

<success_criteria>
- Frontend supports dual-selector branch assignment per D-01
- UI surfaces all assigned branches per D-02
- Global LocationContext filter remains untouched per D-03
</success_criteria>

<output>
After completion, create `.planning/phases/03-architecture-shifts/03-02-SUMMARY.md`
</output>
