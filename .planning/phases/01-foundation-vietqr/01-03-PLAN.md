---
phase: 01-foundation-vietqr
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - website/src/components/payment/VietQrModal.tsx
  - website/src/components/payment/VietQrModal.test.tsx
  - website/src/components/payment/PaymentForm.tsx
  - website/src/components/payment/DepositWallet.tsx
autonomous: true
requirements:
  - PAY-01
  - PAY-03
must_haves:
  truths:
    - "VietQrModal renders a split-panel layout with inputs left and QR image right"
    - "PaymentForm has a 'Tạo QR' button that opens VietQrModal"
    - "DepositWallet offers 'VietQR' as a deposit method that opens VietQrModal"
  artifacts:
    - path: website/src/components/payment/VietQrModal.tsx
      provides: "Split-panel QR generation modal"
      contains: "buildVietQrUrl"
    - path: website/src/components/payment/PaymentForm.tsx
      provides: "Payment form with VietQR integration"
      contains: "VietQrModal"
    - path: website/src/components/payment/DepositWallet.tsx
      provides: "Deposit wallet with VietQR integration"
      contains: "VietQrModal"
  key_links:
    - from: website/src/lib/vietqr.ts
      to: website/src/components/payment/VietQrModal.tsx
      via: "import { buildVietQrUrl, generatePaymentDescription }"
    - from: website/src/hooks/useBankSettings.ts
      to: website/src/components/payment/VietQrModal.tsx
      via: "import { useBankSettings }"
    - from: website/src/components/payment/VietQrModal.tsx
      to: website/src/components/payment/PaymentForm.tsx
      via: "<VietQrModal open={showVietQr} ... />"
---

<objective>
Build the VietQR modal component and integrate it into PaymentForm and DepositWallet.

Purpose: Allow receptionists to generate VietQR codes during payment and deposit flows.
Output: Working VietQrModal integrated into both payment surfaces.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@website/src/components/payment/PaymentForm.tsx
@website/src/components/payment/DepositWallet.tsx
@website/src/lib/vietqr.ts
@website/src/hooks/useBankSettings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: VietQrModal Component + Integration Tests</name>
  <files>website/src/components/payment/VietQrModal.tsx, website/src/components/payment/VietQrModal.test.tsx</files>
  <read_first>website/src/components/payment/PaymentForm.tsx, website/src/components/forms/AddCustomerForm/AddCustomerForm.tsx, website/src/lib/vietqr.ts, website/src/hooks/useBankSettings.ts</read_first>
  <action>
Create `website/src/components/payment/VietQrModal.tsx`:
- Props: `{ open: boolean; onClose: () => void; customerName?: string; customerPhone?: string; defaultAmount?: number }`
- Use `useBankSettings()` to fetch clinic bank configuration
- Layout: outer `div` with `className` containing `grid grid-cols-1 md:grid-cols-2` for split panel
  - Left panel (inputs):
    - Label "Số tiền" with number input for amount (VND)
    - Label "Nội dung" with text input for description
      - Auto-populate on open: if `customerName` provided, set description to `generatePaymentDescription(customerName, customerPhone || '')`
    - Button "Tạo QR" that triggers QR image render
  - Right panel (QR display):
    - If bank settings not loaded: show "Đang tải..."
    - If no bank settings configured: show "Vui lòng cấu hình tài khoản ngân hàng trong Cài đặt"
    - If generated: render `<img src={buildVietQrUrl(...)} alt="VietQR" />`
- Use existing Tailwind modal overlay pattern (backdrop + centered card)

Add `@crossref` block at top:
```typescript
/**
 * @crossref:used-in[PaymentForm.tsx, DepositWallet.tsx]
 * @crossref:uses[vietqr.ts, useBankSettings.ts]
 */
```

Create `website/src/components/payment/VietQrModal.test.tsx`:
1. Mock `useBankSettings` to return `{ settings: { bankBin: '970415', bankNumber: '8815251137', bankAccountName: 'NGUYEN VAN A' }, loading: false, error: null, refresh: jest.fn(), updateSettings: jest.fn() }`
2. Render `<VietQrModal open={true} onClose={jest.fn()} customerName="LAN TRAN" customerPhone="09xx1234" />`
3. Assert description input has value `"LATR1234"`
4. Type `500000` into amount input, click button with text matching `/Tạo QR/i`
5. Assert an `<img>` element exists with `src` containing `img.vietqr.io`
  </action>
  <verify>npm test -- VietQrModal.test.tsx</verify>
  <acceptance_criteria>
    - `website/src/components/payment/VietQrModal.tsx` exists and exports the component
    - `website/src/components/payment/VietQrModal.test.tsx` exists with at least 2 test blocks
    - Component imports `buildVietQrUrl` and `generatePaymentDescription` from `../../lib/vietqr`
    - Component imports `useBankSettings` from `../../hooks/useBankSettings`
    - Component's root layout element has `className` containing `grid` and either `grid-cols-` or `flex`
    - Running `npm test -- VietQrModal.test.tsx` in `website/` exits with code 0
  </acceptance_criteria>
  <done>VietQrModal component and tests are green</done>
</task>

<task type="auto">
  <name>Task 2: Integrate VietQrModal into PaymentForm</name>
  <files>website/src/components/payment/PaymentForm.tsx</files>
  <read_first>website/src/components/payment/PaymentForm.tsx, website/src/components/payment/VietQrModal.tsx</read_first>
  <action>
Modify `website/src/components/payment/PaymentForm.tsx`:
- Add `import { VietQrModal } from './VietQrModal';`
- Add state: `const [showVietQr, setShowVietQr] = useState(false);`
- Locate the bank transfer section (search for text containing "Chuyển khoản" or "Bank transfer")
- Add a button with text "Tạo QR" inside or adjacent to the bank transfer section
- Set `onClick={() => setShowVietQr(true)}` on the button
- Add `<VietQrModal open={showVietQr} onClose={() => setShowVietQr(false)} defaultAmount={...} />` at the end of the component's returned JSX (before closing root tag)
- If customer name/phone is available in PaymentForm props or context, pass them as `customerName` and `customerPhone`
  </action>
  <verify>npm run build in website/ exits 0</verify>
  <acceptance_criteria>
    - `PaymentForm.tsx` imports `VietQrModal` from `./VietQrModal`
    - `PaymentForm.tsx` contains `const [showVietQr, setShowVietQr] = useState(false)`
    - `PaymentForm.tsx` renders `<VietQrModal` with prop `open={showVietQr}`
    - Bank transfer section contains a `<button` with text matching `/Tạo QR/i`
  </acceptance_criteria>
  <done>VietQrModal integrated into PaymentForm</done>
</task>

<task type="auto">
  <name>Task 3: Integrate VietQrModal into DepositWallet</name>
  <files>website/src/components/payment/DepositWallet.tsx</files>
  <read_first>website/src/components/payment/DepositWallet.tsx, website/src/components/payment/VietQrModal.tsx</read_first>
  <action>
Modify `website/src/components/payment/DepositWallet.tsx`:
- Add `import { VietQrModal } from './VietQrModal';`
- Add state: `const [showVietQr, setShowVietQr] = useState(false);`
- Locate existing deposit method options (toggles/buttons)
- Add a new method option with label "VietQR"
- When VietQR method is selected, show a button "Tạo QR" that calls `setShowVietQr(true)`
- Render `<VietQrModal open={showVietQr} onClose={() => setShowVietQr(false)} />` in the component
  </action>
  <verify>npm run build in website/ exits 0</verify>
  <acceptance_criteria>
    - `DepositWallet.tsx` imports `VietQrModal` from `./VietQrModal`
    - `DepositWallet.tsx` contains `const [showVietQr, setShowVietQr] = useState(false)`
    - Deposit method options include text matching `/VietQR/i`
    - `DepositWallet.tsx` renders `<VietQrModal` with prop `open={showVietQr}`
  </acceptance_criteria>
  <done>VietQrModal integrated into DepositWallet</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- VietQrModal.test.tsx` passes
- [ ] `npm run build` succeeds after PaymentForm and DepositWallet modifications
</verification>

<success_criteria>
- All tasks completed
- VietQrModal renders correctly in both payment contexts
- QR image generates with correct VietQR URL
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-vietqr/01-03-SUMMARY.md`
</output>
