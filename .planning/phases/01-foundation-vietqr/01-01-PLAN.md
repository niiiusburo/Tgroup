---
phase: 01-foundation-vietqr
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - website/src/lib/vietqr.ts
  - website/src/lib/vietqr.test.ts
  - website/src/hooks/useBankSettings.ts
  - website/src/hooks/useBankSettings.test.ts
autonomous: true
requirements:
  - PAY-01
  - PAY-04
  - DATA-04
must_haves:
  truths:
    - "VietQR URL builder produces correct img.vietqr.io URLs with encoded parameters"
    - "Payment description auto-generates from customer name initials + phone last 4 digits"
    - "useBankSettings hook fetches and updates clinic bank account via GET/PUT /api/settings/bank"
  artifacts:
    - path: website/src/lib/vietqr.ts
      provides: "VietQR URL builder and description generator"
      contains: "export function buildVietQrUrl"
    - path: website/src/hooks/useBankSettings.ts
      provides: "React hook for bank settings CRUD"
      contains: "export function useBankSettings"
  key_links:
    - from: website/src/lib/vietqr.ts
      to: website/src/hooks/useBankSettings.ts
      via: "independent utilities consumed by VietQrModal"
---

<objective>
Create foundational utilities for VietQR URL generation and the bank settings data hook using TDD.

Purpose: These libraries power all VietQR UI in later plans.
Output: `vietqr.ts`, `vietqr.test.ts`, `useBankSettings.ts`, `useBankSettings.test.ts`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@website/src/lib/api.ts
@website/src/hooks/useCustomers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: VietQR URL Builder + Unit Tests</name>
  <files>website/src/lib/vietqr.ts, website/src/lib/vietqr.test.ts</files>
  <read_first>website/src/lib/api.ts, website/package.json</read_first>
  <action>
Create `website/src/lib/vietqr.ts`:
- `buildVietQrUrl({ bin, number, amount, description, name }: { bin: string; number: string; amount: number; description: string; name: string }): string`
  - Returns exactly: `https://img.vietqr.io/image/${bin}-${number}-compact.jpg?amount=${amount}&amp;addInfo=${encodeURIComponent(description)}&amp;accountName=${encodeURIComponent(name)}`
- `generatePaymentDescription(customerName: string, phone: string): string`
  1. If `customerName` is empty or whitespace, return empty string `''`
  2. Split name by whitespace, take first character of each part, uppercase it
  3. Concatenate initials, then append last 4 digits of `phone` (or all digits if fewer than 4)
  4. Examples: `'LAN TRAN'` + `'09xx1234'` → `'LATR1234'`; `'NGUYEN VAN A'` + `'123'` → `'NVA123'`

Create `website/src/lib/vietqr.test.ts` with these test cases:
1. `buildVietQrUrl({ bin: '970415', number: '8815251137', amount: 1000000, description: 'LATR1234', name: 'NGUYEN VAN A' })` returns string containing `img.vietqr.io/image/970415-8815251137-compact.jpg?amount=1000000&amp;addInfo=LATR1234&amp;accountName=NGUYEN%20VAN%20A`
2. `generatePaymentDescription('LAN TRAN', '09xx1234')` returns `'LATR1234'`
3. `generatePaymentDescription('', '09xx1234')` returns `''`
4. `generatePaymentDescription('NGUYEN VAN A', '123')` returns `'NVA123'`
  </action>
  <verify>npm test -- vietqr.test.ts</verify>
  <acceptance_criteria>
    - `website/src/lib/vietqr.ts` exists and exports `buildVietQrUrl` and `generatePaymentDescription`
    - `website/src/lib/vietqr.test.ts` exists with at least 4 test blocks
    - Running `npm test -- vietqr.test.ts` in `website/` exits with code 0
    - `buildVietQrUrl` uses `encodeURIComponent(description)` and `encodeURIComponent(name)`
  </acceptance_criteria>
  <done>VietQR library and tests are green</done>
</task>

<task type="auto">
  <name>Task 2: useBankSettings Hook + Unit Tests</name>
  <files>website/src/hooks/useBankSettings.ts, website/src/hooks/useBankSettings.test.ts</files>
  <read_first>website/src/lib/api.ts, website/src/hooks/useCustomers.ts</read_first>
  <action>
Create `website/src/hooks/useBankSettings.ts`:
- Type `BankSettings = { bankBin: string; bankNumber: string; bankAccountName: string }`
- Hook signature: `useBankSettings()` returns `{ settings: BankSettings | null; loading: boolean; error: Error | null; refresh: () => Promise&lt;void&gt;; updateSettings: (data: BankSettings) => Promise&lt;void&gt; }`
- `refresh()` calls `GET /api/settings/bank` via the same fetch pattern as `useCustomers.ts`
- `updateSettings(data)` calls `PUT /api/settings/bank` with JSON body `{ bankBin: data.bankBin, bankNumber: data.bankNumber, bankAccountName: data.bankAccountName }`
- On successful update, call `refresh()` to update local state

Create `website/src/hooks/useBankSettings.test.ts`:
1. Mock `global.fetch` to return JSON `{ bankBin: '970415', bankNumber: '8815251137', bankAccountName: 'NGUYEN VAN A' }` on GET
2. Render hook, wait for effect, assert `result.current.settings` equals mock object and `loading` becomes `false`
3. Mock PUT response `{ success: true }`, call `result.current.updateSettings({ bankBin: '970436', bankNumber: '123456789', bankAccountName: 'TRAN THI B' })`, assert `fetch` was called with url containing `/api/settings/bank` and method `'PUT'`
  </action>
  <verify>npm test -- useBankSettings.test.ts</verify>
  <acceptance_criteria>
    - `website/src/hooks/useBankSettings.ts` exists and exports `useBankSettings`
    - `website/src/hooks/useBankSettings.test.ts` exists with at least 3 test blocks
    - Running `npm test -- useBankSettings.test.ts` in `website/` exits with code 0
    - Hook's `refresh` calls endpoint matching `/api/settings/bank` with method `GET`
    - Hook's `updateSettings` calls endpoint matching `/api/settings/bank` with method `PUT` and JSON body containing `bankBin`, `bankNumber`, `bankAccountName`
  </acceptance_criteria>
  <done>useBankSettings hook and tests are green</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- vietqr.test.ts` passes
- [ ] `npm test -- useBankSettings.test.ts` passes
</verification>

<success_criteria>
- All tasks completed
- All TDD unit tests pass
- No type errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-vietqr/01-01-SUMMARY.md`
</output>
