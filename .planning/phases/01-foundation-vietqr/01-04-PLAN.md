---
phase: 01-foundation-vietqr
plan: "04"
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - website/src/lib/api.ts
  - website/src/components/payment/VietQrModal.tsx
  - api/src/routes/payments.js
  - api/src/server.js
  - api/migrations/002_payment_proofs.sql
  - website/e2e/vietqr-payment.spec.ts
  - website/package.json
  - website/public/CHANGELOG.json
autonomous: true
requirements:
  - PAY-02
must_haves:
  truths:
    - "User can upload a payment proof image after QR generation"
    - "Payment proof is stored in a dedicated payment_proofs table"
    - "E2E test TC-VQ1 passes: navigate to Payments, click Tạo QR, enter amount, assert QR visible"
    - "website/package.json version is bumped and CHANGELOG.json has a new entry"
  artifacts:
    - path: api/migrations/002_payment_proofs.sql
      provides: "DB schema for payment proofs"
      contains: "CREATE TABLE IF NOT EXISTS payment_proofs"
    - path: website/e2e/vietqr-payment.spec.ts
      provides: "Playwright E2E test for VietQR"
      contains: "img.vietqr.io"
    - path: website/public/CHANGELOG.json
      provides: "Release notes for v0.4.10"
      contains: "VietQR"
  key_links:
    - from: api/src/routes/payments.js
      to: website/src/components/payment/VietQrModal.tsx
      via: "POST /api/payments/:id/proof consumed by upload handler"
---

<objective>
Add payment proof upload flow behind the QR modal, write the E2E test, and bump the version.

Purpose: Close the loop on QR payments (generate → pay → upload proof) and verify the full flow end-to-end.
Output: Payment proof backend + UI, passing E2E test, updated version and changelog.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@website/src/components/payment/VietQrModal.tsx
@website/src/lib/api.ts
@api/src/routes/payments.js
@api/src/server.js
@website/e2e/login-and-settings.spec.ts
@website/package.json
@website/public/CHANGELOG.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Payment Proof Backend</name>
  <files>api/migrations/002_payment_proofs.sql, api/src/routes/payments.js, api/src/server.js</files>
  <read_first>api/src/routes/bankSettings.js, api/src/server.js, CLAUDE.md</read_first>
  <action>
Create `api/migrations/002_payment_proofs.sql`:
```sql
CREATE TABLE IF NOT EXISTS payment_proofs (
  id SERIAL PRIMARY KEY,
  payment_id INTEGER,
  proof_image TEXT NOT NULL,
  qr_description TEXT,
  qr_generated_at TIMESTAMP DEFAULT NOW(),
  created_at TIMESTAMP DEFAULT NOW()
);
```

Create `api/src/routes/payments.js`:
- Define an Express router
- `POST /:id/proof`:
  1. Extract `id` from params and `proofImageBase64` from body
  2. Validate `proofImageBase64` is a non-empty string starting with `data:image/`; if not, return 400
  3. Insert: `INSERT INTO payment_proofs (payment_id, proof_image, qr_description) VALUES ($1, $2, $3) RETURNING *` where `payment_id` is the `:id` param
  4. Return JSON `{ success: true, proofId: row.id }`

Modify `api/src/server.js`:
- Add `app.use('/api/payments', require('./routes/payments'));` near other route registrations.
  </action>
  <verify>PGPASSWORD=postgres psql -h 127.0.0.1 -p 55433 -U postgres -d tdental_demo -f api/migrations/002_payment_proofs.sql</verify>
  <acceptance_criteria>
    - `api/migrations/002_payment_proofs.sql` exists and contains `CREATE TABLE IF NOT EXISTS payment_proofs`
    - Table has columns `payment_id INTEGER`, `proof_image TEXT`, `qr_description TEXT`
    - `api/src/routes/payments.js` exists and exports an Express router with `router.post('/:id/proof'`
    - `api/src/server.js` contains `require('./routes/payments')` mounted at path matching `/api/payments`
    - Endpoint returns JSON with `success: true` and `proofId` on valid input
    - Endpoint returns 400 if `proofImageBase64` does not start with `data:image/`
  </acceptance_criteria>
  <done>Payment proof backend and DB table ready</done>
</task>

<task type="auto">
  <name>Task 2: Payment Proof Upload UI in VietQrModal</name>
  <files>website/src/components/payment/VietQrModal.tsx, website/src/lib/api.ts</files>
  <read_first>website/src/components/payment/VietQrModal.tsx, website/src/lib/api.ts</read_first>
  <action>
Add to `website/src/components/payment/VietQrModal.tsx`:
- After the QR image display (right panel, below the QR), add a section with:
  - Label "Ảnh xác nhận chuyển khoản" / "Payment proof"
  - `<input type="file" accept="image/*" />` for selecting an image file
  - When a file is selected, read it with `FileReader` and store the base64 result in component state
  - Preview thumbnail using `<img src={base64Preview} />`
  - Button "Xác nhận đã thanh toán" / "Confirm payment"
    - On click, call a new API helper `uploadPaymentProof` (add to `website/src/lib/api.ts`)
    - Show success text by rendering a div with `className` containing `bg-green` or `text-green-600` and text matching `/Đã lưu|Saved|thành công|success/i`

Create `uploadPaymentProof` in `website/src/lib/api.ts`:
- `uploadPaymentProof(paymentId: string, proofImageBase64: string): Promise<{ success: boolean; proofId?: number }>`
- Calls `POST /api/payments/${paymentId}/proof` with JSON body `{ proofImageBase64 }`
  </action>
  <verify>npm run build in website/ exits 0</verify>
  <acceptance_criteria>
    - `VietQrModal.tsx` contains `<input type="file" accept="image/*" />` for proof upload
    - `VietQrModal.tsx` renders a `<button` with text matching `/Xác nhận|Confirm payment/i`
    - `website/src/lib/api.ts` exports `uploadPaymentProof` function
    - After successful upload, modal shows an element with `className` containing `green` and text matching `/Đã lưu|Saved|thành công|success/i`
  </acceptance_criteria>
  <done>Payment proof upload UI functional</done>
</task>

<task type="auto">
  <name>Task 3: E2E Test TC-VQ1</name>
  <files>website/e2e/vietqr-payment.spec.ts</files>
  <read_first>website/e2e/login-and-settings.spec.ts, website/e2e/customer-profile-crud.spec.ts, CLAUDE.md</read_first>
  <action>
Create `website/e2e/vietqr-payment.spec.ts`:
1. Import test utilities from `@playwright/test`
2. Use existing auth setup or login manually:
   - Navigate to `/login`
   - Fill email `tg@clinic.vn` and password `123456`
   - Click login button, wait for navigation to `/overview`
3. Navigate to `/customers`
4. Click on a customer row to open their profile
5. Click the "Payments" tab
6. Click button with text matching `/Tạo QR/i`
7. Fill amount input with `500000`
8. Click button with text matching `/Tạo QR/i` inside the modal
9. Assert that an `<img>` with `src` containing `img.vietqr.io` is visible using `.locator('img[src*="img.vietqr.io"]').isVisible()`
  </action>
  <verify>npx playwright test vietqr-payment.spec.ts</verify>
  <acceptance_criteria>
    - `website/e2e/vietqr-payment.spec.ts` exists
    - Test uses credentials `tg@clinic.vn` and `123456`
    - Test navigates to customer profile, opens Payments, clicks Tạo QR, enters amount, asserts QR img visible
    - Running `npx playwright test vietqr-payment.spec.ts` in `website/` exits with code 0
  </acceptance_criteria>
  <done>E2E test TC-VQ1 passes</done>
</task>

<task type="auto">
  <name>Task 4: Version Bump and CHANGELOG Update</name>
  <files>website/package.json, website/public/CHANGELOG.json</files>
  <read_first>website/package.json, website/public/CHANGELOG.json, CLAUDE.md</read_first>
  <action>
1. Read `website/package.json`, bump the `version` field to next patch (e.g., `0.4.9` → `0.4.10`)
2. Run `git rev-parse --short HEAD` to get current commit hash
3. Read `website/public/CHANGELOG.json`, add a new entry at the TOP of the array:
```json
{
  "version": "0.4.10",
  "date": "2026-04-10",
  "commit": "[commit hash from git rev-parse --short HEAD]",
  "highlights": "VietQR payment generation and clinic bank account configuration",
  "sections": [
    {
      "title": "New Features",
      "items": [
        "VietQR code generation in PaymentForm and DepositWallet",
        "Clinic bank account configuration via Settings API",
        "Payment proof upload after QR transfer"
      ]
    },
    {
      "title": "Testing",
      "items": [
        "TDD unit tests for VietQR builder and bank settings hook",
        "Playwright E2E test TC-VQ1 for QR payment flow"
      ]
    }
  ]
}
```
  </action>
  <verify>git diff --name-only | grep -E "package\.json|CHANGELOG\.json"</verify>
  <acceptance_criteria>
    - `website/package.json` `version` field is incremented from its previous value
    - `website/public/CHANGELOG.json` exists and has a new top entry with `version` matching package.json
    - CHANGELOG entry `date` is `2026-04-10`
    - CHANGELOG contains at least one section titled `"New Features"` with items mentioning `"VietQR"`
  </acceptance_criteria>
  <done>Version and changelog updated</done>
</task>

</tasks>

<verification>
- [ ] `npx playwright test vietqr-payment.spec.ts` passes
- [ ] `npm run build` succeeds
- [ ] `package.json` version bumped and `CHANGELOG.json` updated
</verification>

<success_criteria>
- All tasks completed
- Payment proof upload works end-to-end
- E2E test passes
- Release artifacts updated
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-vietqr/01-04-SUMMARY.md`
</output>
