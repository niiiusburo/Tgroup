---
phase: 02-quick-features-validations
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - website/src/pages/Calendar.tsx
  - website/src/hooks/useCalendarData.ts
  - website/src/components/modules/PatientCheckIn.tsx
  - website/src/hooks/useOverviewAppointments.ts
  - website/e2e/phase2-quick-features.spec.ts
  - website/package.json
  - website/public/CHANGELOG.json
autonomous: true
requirements:
  - "#8 3 quick search boxes"
  - "#9 Countdown timer integration"
  - "#10 Quick-add customer"
must_haves:
  truths:
    - Calendar page shows 3 independent quick search boxes (patient, doctor, service)
    - PatientCheckIn cards display a WaitTimer for waiting-status patients
    - Playwright E2E tests verify the new fields and quick-add customer flow
  artifacts:
    - path: website/src/pages/Calendar.tsx
      provides: Three search inputs on toolbar
      contains: "patientSearch"
    - path: website/src/hooks/useCalendarData.ts
      provides: Split search state and filtered logic
      contains: "patientSearch"
    - path: website/src/components/modules/PatientCheckIn.tsx
      provides: WaitTimer rendered in PatientCard
      contains: "WaitTimer"
    - path: website/e2e/phase2-quick-features.spec.ts
      provides: Phase 2 E2E coverage
      contains: "customer code"
  key_links:
    - from: Calendar.tsx
      to: useCalendarData.ts
      via: three search setters
      pattern: "setPatientSearch"
---

<objective>
Add 3 quick search boxes to the Calendar page, integrate WaitTimer into Overview PatientCheckIn cards, and write Playwright E2E tests covering all Phase 2 features plus version bump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@website/src/pages/Calendar.tsx
@website/src/hooks/useCalendarData.ts
@website/src/components/modules/PatientCheckIn.tsx
@website/src/hooks/useOverviewAppointments.ts
@website/src/components/appointments/WaitTimer.tsx
@website/package.json
@website/public/CHANGELOG.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calendar page 3 quick search boxes (patient, doctor, service)</name>
  <files>website/src/hooks/useCalendarData.ts, website/src/pages/Calendar.tsx</files>
  <read_first>
    - website/src/hooks/useCalendarData.ts
    - website/src/pages/Calendar.tsx
  </read_first>
  <action>
    1. In `website/src/hooks/useCalendarData.ts`:
       - Replace the single `searchTerm` / `setSearchTerm` state with three states:
         ```typescript
         const [patientSearch, setPatientSearch] = useState('');
         const [doctorSearch, setDoctorSearch] = useState('');
         const [serviceSearch, setServiceSearch] = useState('');
         ```
       - Update `filteredAppointments` logic:
         ```typescript
         const matchesPatient = patientSearchTerm
           ? normalizeText(apt.customerName).includes(patientSearchTerm) ||
             normalizeText(apt.customerPhone).includes(patientSearchTerm)
           : true;
         const matchesDoctor = doctorSearchTerm
           ? normalizeText(apt.dentist).includes(doctorSearchTerm)
           : true;
         const matchesService = serviceSearchTerm
           ? normalizeText(apt.serviceName || '').includes(serviceSearchTerm)
           : true;
         return matchesDoctor && matchesStatus && matchesPatient && matchesDoctor && matchesService;
         ```
         (Use local consts for the normalized terms from the memo dependencies.)
       - Return the six new values instead of `searchTerm` / `setSearchTerm`:
         ```typescript
         patientSearch, setPatientSearch,
         doctorSearch, setDoctorSearch,
         serviceSearch, setServiceSearch,
         ```

    2. In `website/src/pages/Calendar.tsx`:
       - Destructure the six new values from `useCalendarData`.
       - Replace the single search input in the toolbar with three side-by-side inputs:
         ```tsx
         <div className="flex items-center gap-2 w-full lg:w-auto">
           <div className="relative w-full lg:w-40">
             <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400" />
             <input
               type="text"
               value={patientSearch}
               onChange={(e) => setPatientSearch(e.target.value)}
               placeholder="Bệnh nhân..."
               className="w-full pl-9 pr-3 py-1.5 text-sm border border-gray-200 rounded-lg focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary"
             />
           </div>
           <div className="relative w-full lg:w-40">
             <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400" />
             <input
               type="text"
               value={doctorSearch}
               onChange={(e) => setDoctorSearch(e.target.value)}
               placeholder="Bác sĩ..."
               className="w-full pl-9 pr-3 py-1.5 text-sm border border-gray-200 rounded-lg focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary"
             />
           </div>
           <div className="relative w-full lg:w-40">
             <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400" />
             <input
               type="text"
               value={serviceSearch}
               onChange={(e) => setServiceSearch(e.target.value)}
               placeholder="Dịch vụ..."
               className="w-full pl-9 pr-3 py-1.5 text-sm border border-gray-200 rounded-lg focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary"
             />
           </div>
           <QuickAddAppointmentButton onSuccess={refresh} size="sm" />
           <div className="flex-1 lg:flex-none">
             <FilterByDoctor ... />
           </div>
         </div>
         ```
  </action>
  <verify>
    <automated>cd /Users/thuanle/Documents/TamTMV/Tgroup/website && npx tsc --noEmit 2>&1 | grep -E "Calendar|useCalendarData" | wc -l | tr -d ' '</automated>
  </verify>
  <done>
    - useCalendarData exposes patientSearch, doctorSearch, serviceSearch and filters correctly.
    - Calendar toolbar shows three search inputs with Vietnamese placeholders.
  </done>
  <acceptance_criteria>
    - `grep -n "patientSearch" website/src/hooks/useCalendarData.ts` returns lines for state and return.
    - `grep -n "setPatientSearch" website/src/pages/Calendar.tsx` returns a line binding the input.
    - `grep -n "Bệnh nhân" website/src/pages/Calendar.tsx` returns a line.
    - `grep -n "Bác sĩ" website/src/pages/Calendar.tsx` returns a line.
    - `grep -n "Dịch vụ" website/src/pages/Calendar.tsx` returns a line.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Integrate WaitTimer into PatientCheckIn for waiting status</name>
  <files>website/src/hooks/useOverviewAppointments.ts, website/src/components/modules/PatientCheckIn.tsx</files>
  <read_first>
    - website/src/hooks/useOverviewAppointments.ts
    - website/src/components/modules/PatientCheckIn.tsx
    - website/src/components/appointments/WaitTimer.tsx
  </read_first>
  <action>
    1. In `website/src/hooks/useOverviewAppointments.ts`:
       - Add to `OverviewAppointment` interface:
         ```typescript
         readonly arrivalTime: string | null;
         readonly treatmentStartTime: string | null;
         ```
       - In `mapApiToOverview`, set:
         ```typescript
         arrivalTime: apt.time || '09:00',
         treatmentStartTime: checkInStatus === 'waiting' ? null : (apt.time || '09:00'),
         ```

    2. In `website/src/components/modules/PatientCheckIn.tsx`:
       - Import `WaitTimer` from `@/components/appointments/WaitTimer`.
       - Inside `PatientCard`, below the status badge (and outside the dropdown), add:
         ```tsx
         {currentStatus === 'waiting' && appointment.arrivalTime && (
           <div className="mt-2">
             <WaitTimer arrivalTime={appointment.arrivalTime} treatmentStartTime={appointment.treatmentStartTime} compact />
           </div>
         )}
         ```
       - Ensure this is placed after the status badge button and before the patient info `div`, and that it does not break the layout lock (no fixed widths on PatientCard).
  </action>
  <verify>
    <automated>cd /Users/thuanle/Documents/TamTMV/Tgroup/website && npx tsc --noEmit 2>&1 | grep -E "PatientCheckIn|useOverviewAppointments" | wc -l | tr -d ' '</automated>
  </verify>
  <done>
    - OverviewAppointment includes arrivalTime and treatmentStartTime.
    - PatientCheckIn PatientCard renders WaitTimer for waiting status.
  </done>
  <acceptance_criteria>
    - `grep -n "arrivalTime" website/src/hooks/useOverviewAppointments.ts` returns lines in interface and mapper.
    - `grep -n "WaitTimer" website/src/components/modules/PatientCheckIn.tsx` returns an import line and a JSX line.
    - `grep -n "currentStatus === 'waiting'" website/src/components/modules/PatientCheckIn.tsx` returns a line.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E tests, version bump, and CHANGELOG update</name>
  <files>website/e2e/phase2-quick-features.spec.ts, website/package.json, website/public/CHANGELOG.json</files>
  <read_first>
    - website/e2e/customer-profile-crud.spec.ts
    - website/package.json
    - website/public/CHANGELOG.json
  </read_first>
  <action>
    1. Create `website/e2e/phase2-quick-features.spec.ts` with tests covering:
       - TC-ProfileCode: Open a customer profile, assert "Customer Code" label is visible.
       - TC-DuplicatePhone: Attempting to create a customer with an existing phone via API returns 409 (or a quick UI smoke if easier — use `request.post` to `/api/Partners`).
       - TC-ServiceDentalAide: From customer profile Records tab, open Add Service, assert a third selector with placeholder "Chọn nha sĩ phụ..." exists.
       - TC-DepositDate: From customer profile Payment tab, click Add Deposit, assert a `type="date"` input exists and its value is today (`YYYY-MM-DD`).
       - TC-PaymentDate: From customer profile Payment tab, click Make Payment, assert a `type="date"` input exists and defaults to today.
       - TC-CalendarSearch: Go to Calendar page, assert three search placeholders ("Bệnh nhân...", "Bác sĩ...", "Dịch vụ...") exist.
       - TC-QuickAddCustomer: On the Customers list page, click the floating orange + button, assert the AddCustomerForm modal opens (this verifies #10 is already working).
       Use the standard `login` helper pattern from `customer-profile-crud.spec.ts` and test customer "Phạm Ngọc Huy".

    2. Bump `website/package.json` version from `0.4.12` to `0.4.13`.

    3. Update `website/public/CHANGELOG.json`:
       - Add a new entry at the TOP with version `0.4.13`, date `2026-04-10`, git commit hash (use `git rev-parse --short HEAD`), highlights `"Phase 2 quick features and validations"`, and sections:
         - "New Features": items for customer code visibility, duplicate phone check, dental aide selector, 3 calendar search boxes, wait timer, deposit/payment date pickers.
         - "Testing": Added Phase 2 E2E coverage.
  </action>
  <verify>
    <automated>cd /Users/thuanle/Documents/TamTMV/Tgroup/website && npx playwright test e2e/phase2-quick-features.spec.ts --project=chromium 2>&1 | tail -n 20</automated>
  </verify>
  <done>
    - E2E spec exists and passes for Phase 2 features.
    - package.json version is 0.4.13.
    - CHANGELOG.json has a new top entry for 0.4.13.
  </done>
  <acceptance_criteria>
    - `test -f website/e2e/phase2-quick-features.spec.ts` returns true.
    - `grep -n '"version": "0.4.13"' website/package.json` returns a line.
    - `grep -n '"0.4.13"' website/public/CHANGELOG.json` returns a line within the first 10 lines.
    - Playwright test output shows all tests passed.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Calendar toolbar shows three search inputs and filtering works without errors.
2. WaitTimer appears on waiting PatientCheckIn cards.
3. Playwright E2E suite passes for Phase 2.
4. Version and CHANGELOG are updated.
</verification>

<success_criteria>
- Calendar page has 3 quick search boxes filtering patient, doctor, and service independently.
- Overview PatientCheckIn cards show WaitTimer for waiting patients.
- Playwright tests verify each new field, customer code, and quick-add customer flow.
- Version bumped to 0.4.13 and CHANGELOG.json updated.
</success_criteria>

<output>
After completion, create `.planning/phases/02-quick-features-validations/02-03-SUMMARY.md`
</output>
