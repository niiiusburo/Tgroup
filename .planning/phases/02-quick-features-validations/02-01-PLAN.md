---
phase: 02-quick-features-validations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/routes/partners.js
  - website/src/hooks/useCustomerProfile.ts
  - website/src/components/customer/CustomerProfile.tsx
autonomous: true
requirements:
  - "#4 Customer code visibility"
  - "#5 Duplicate phone check"
must_haves:
  truths:
    - Customer code is visible in the read-only Customer Profile view
    - API POST /api/Partners rejects duplicate phone numbers with HTTP 409 Conflict
  artifacts:
    - path: api/src/routes/partners.js
      provides: Duplicate phone validation
      contains: "SELECT id FROM partners WHERE phone ="
    - path: website/src/hooks/useCustomerProfile.ts
      provides: CustomerProfileData includes code
      contains: "code:"
    - path: website/src/components/customer/CustomerProfile.tsx
      provides: Code rendered in Personal Information grid
      contains: "Customer Code"
  key_links:
    - from: useCustomerProfile.ts
      to: CustomerProfile.tsx
      via: profile.code prop
      pattern: "profile\\.code"
---

<objective>
Add backend duplicate-phone validation and surface the customer code in the read-only Customer Profile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@api/src/routes/partners.js
@website/src/hooks/useCustomerProfile.ts
@website/src/components/customer/CustomerProfile.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Partners POST rejects duplicate phone with 409 Conflict</name>
  <files>api/src/routes/partners.js</files>
  <read_first>
    - api/src/routes/partners.js
  </read_first>
  <behavior>
    - Existing POST /api/Partners currently allows duplicate phone numbers.
    - It must query the DB for an existing partner with the same phone (case-sensitive or normalized) before inserting.
    - If a duplicate exists, respond with status 409 and JSON `{ error: "Phone number already exists" }`.
  </behavior>
  <action>
    In `api/src/routes/partners.js`, inside the `router.post('/', ...)` handler, after the "Name and phone are required" check and before `// Generate a new UUID`, insert:

    ```javascript
    const duplicate = await query(
      'SELECT id FROM partners WHERE phone = $1 AND isdeleted = false LIMIT 1',
      [phone]
    );
    if (duplicate && duplicate.length > 0) {
      return res.status(409).json({ error: 'Phone number already exists' });
    }
    ```

    Do not change any other behavior.
  </action>
  <verify>
    <automated>cd /Users/thuanle/Documents/TamTMV/Tgroup/api && node -e "const r = require('./src/routes/partners.js'); console.log('loaded')"</automated>
  </verify>
  <done>
    - POST handler contains the duplicate phone SELECT query.
    - On duplicate, returns 409 with message "Phone number already exists".
  </done>
  <acceptance_criteria>
    - `grep -n "SELECT id FROM partners WHERE phone =" api/src/routes/partners.js` returns a line inside the POST handler.
    - `grep -n "Phone number already exists" api/src/routes/partners.js` returns a line.
    - `grep -n "status(409)" api/src/routes/partners.js` returns a line.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Expose partner code in useCustomerProfile hook</name>
  <files>website/src/hooks/useCustomerProfile.ts</files>
  <read_first>
    - website/src/hooks/useCustomerProfile.ts
  </read_first>
  <action>
    Update `website/src/hooks/useCustomerProfile.ts`:
    1. Add `code: string;` to the `CustomerProfileData` interface.
    2. In the `profileData` object inside `fetchProfile`, add `code: partner.code ?? '',`.
  </action>
  <verify>
    <automated>cd /Users/thuanle/Documents/TamTMV/Tgroup/website && npx tsc --noEmit 2>&1 | grep -c "useCustomerProfile" || echo 0</automated>
  </verify>
  <done>
    - CustomerProfileData interface contains `code: string`.
    - profileData object sets `code` from `partner.code`.
  </done>
  <acceptance_criteria>
    - `grep -n "code: string" website/src/hooks/useCustomerProfile.ts` returns at least one line in the interface.
    - `grep -n "partner.code" website/src/hooks/useCustomerProfile.ts` returns a line assigning code.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Render customer code in CustomerProfile read-only view</name>
  <files>website/src/components/customer/CustomerProfile.tsx</files>
  <read_first>
    - website/src/components/customer/CustomerProfile.tsx
  </read_first>
  <action>
    In `website/src/components/customer/CustomerProfile.tsx`, find the `activeTab === 'profile'` grid and add a new cell showing the customer code. Insert it as the first item in the grid (before Full Name):

    ```tsx
    <div>
      <p className="text-xs text-gray-400">Customer Code</p>
      <p className="text-sm font-medium text-gray-900">{profile.code || 'N/A'}</p>
    </div>
    ```
  </action>
  <verify>
    <automated>cd /Users/thuanle/Documents/TamTMV/Tgroup/website && npx tsc --noEmit 2>&1 | grep "CustomerProfile" | wc -l | tr -d ' '</automated>
  </verify>
  <done>
    - Customer Code appears in the Personal Information grid.
    - TypeScript compiles without errors.
  </done>
  <acceptance_criteria>
    - `grep -n "Customer Code" website/src/components/customer/CustomerProfile.tsx` returns a line.
    - `grep -n "profile.code" website/src/components/customer/CustomerProfile.tsx` returns a line.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Backend POST duplicate check returns 409.
2. Frontend CustomerProfile displays code field.
3. TypeScript type-check passes across modified files.
</verification>

<success_criteria>
- API rejects duplicate phone with 409 Conflict.
- Customer code is visible in CustomerProfile read-only Personal Information grid.
</success_criteria>

<output>
After completion, create `.planning/phases/02-quick-features-validations/02-01-SUMMARY.md`
</output>
