# Website Clone

This is a complete clone of: https://tamdentist.tdental.vn/#/partners/customers/df9ae0c6-4f17-41fb-a2ec-b42c00fb7701/teeth-status
Cloned on: {{TIMESTAMP}}

## Files Included

- **clone.html** - The main clone with HTML corrections applied (recommended)
- **clone-original.html** - The raw HTML exactly as captured (may have display issues)
- **assets/** - All images, stylesheets, and other resources

## Quick Start

### Option 1: Using Node.js (Recommended)

1. Install Node.js if you haven't already
2. Open terminal in this folder
3. Run:
   ```bash
   npm install
   npm start
   ```
   This will start a local server and open the clone in your browser.

### Option 2: Using Python

Run the Python server script:
```bash
python3 start.py
```
Or on Windows:
```bash
python start.py
```

### Option 3: Direct File Access

Simply open `clone.html` directly in your browser. Note that some features might not work due to CORS restrictions.

## Alternative Commands

- `npm run start:original` - Opens the original (uncorrected) version
- `npm run server` - Uses http-server instead of serve

## About HTML Corrections

The main `clone.html` file has automatic corrections applied to fix common HTML issues like:
- Nested buttons (converted to div with role="button")
- Invalid element nesting
- Other HTML5 validation issues

The `clone-original.html` preserves the exact original structure without corrections.

## Troubleshooting

If the site doesn't display correctly:
1. Make sure all files in the assets folder are present
2. Try using a local server instead of opening files directly
3. Check browser console for any errors
4. Some dynamic features may not work in the static clone

---
Generated by UI2 Chrome Extension