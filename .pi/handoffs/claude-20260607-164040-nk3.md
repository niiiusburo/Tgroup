# Handoff: NK3 hardening + spec-wave completion + items 1/3 deploy

**Handoff ID:** claude-20260607-164040-nk3
**Date:** 2026-06-07
**Session Tool:** Claude
**Session ID:** n/a
**Branch:** nk3-deploy
**Worktree:** /Users/thuanle/Documents/TamTMV/Tgrouptest

## What we were doing
Multi-day NK3 (tmv.2checkin.com) work: fixed the CTV portal 403, hardened cross-LOB isolation,
verified the full CTV-referral-commission spec waves, then shipped two follow-ups (null-level
earnings backfill + FE polish) to **NK3 only**. NK/NK2 deliberately untouched throughout.

## Done + LIVE on NK3 (verified)
- CTV portal 403 fix: permissionService grants ctv.* regardless of tier_id (is_ctv short-circuit).
- Cross-LOB PHI gate: api/src/middleware/dentalLobGate.js (INV-008F). cosmetic/CTV → dental data routes = 403.
- 5 dead routes (Commissions/Receipts/AccountJournals/StockPickings/HrPayslips) 500→404.
- migrations applied to both NK3 DBs: 059 (CTV never staff tier), 060 (earnings.payout_id FK),
  061 (backfill level=0 for legacy single-level null earnings — fixed 3 tcosmetic_nk3 rows).
- telemetry/version 401/500 → 200 (PUBLIC_PATHS + public.version_events schema-qualify).
- Dockerfile.web now bakes VITE_PAYMENT_EDIT_DISABLED + VITE_CTV_PUBLIC_ROOT_SIGNUP (were inert).
- Spec waves 1-8 all implemented+verified (4 combined-payouts, 6 hierarchy-move, 7 deposit-history,
  8 payment-edit removal). See docs/business-logic/ctv-referral-commission*.md + docs/NK3-HARDENING-PRD.md.
- Item 3 FE polish (LIVE in web 0.32.112): CtvLinkBar ≤7d AlertTriangle urgency icon; LOB labels
  i18n'd via common.json lob.dental/lob.cosmetic (vi: Nha khoa/Thẩm mỹ) wired in FilterByBusinessUnit,
  EarningsPayoutsTabs(x2), NewClientsTab. Verified live: VI shows "Thẩm mỹ", no raw-key leak.

## Current state
WORKING. Live: web 0.32.112, api patched (gate/dead-routes/telemetry). All my backend changes also
copied into /opt/tgroup-nk3/app so they survive a rebuild. Commits 4e616a3a..a999dd39 on nk3-deploy.

## Verification results
- api tests: 55/55 on touched suites; full baseline 971 pass / 7 pre-existing env failures.
- web: tsc clean; 31 commission/ctv/CtvLinkBar vitest pass.
- Live matrix: PHI gate 403, admin 200, dead routes 404, telemetry 200, combined-payout 400,
  CTV create 201, hierarchy-move guard 409, deposit-edit buttons 0/4 rows, VI "Thẩm mỹ" renders.

## Blockers / open questions
- 3 null-level earnings: RESOLVED (migration 061 → level 0). None remaining.
- NK2/NK promotion: NOT started — needs explicit user go-ahead (production).
- Optional: confirm VI wording "Nha khoa/Thẩm mỹ" with operator.

## Next steps
1. Real-device VI QA on tmv.2checkin.com (LOB dropdown wording + CtvLinkBar ≤7d icon).
2. Promote NK3 hardening + items 1/3 to NK2 → NK on user go-ahead (run tier_id distribution check on *_demo first).
3. Optional FE follow-ups remain low-value (none blocking).

## Context to preserve
- NK3 DBs: tdental_nk3 / tcosmetic_nk3 on tgroup-db (NOT *_smoketest, NOT *_demo). Containers tgroup-nk3-{web,api}.
- Deploy: api via docker cp + restart; web via `cd /opt/tgroup-nk3/runtime && docker compose -f docker-compose.nk3.yml -p runtime build web && up -d web` (tsc-gated, old image stays on fail).
- VITE_ flags must be ARG/ENV + in Dockerfile.web .env.production.local printf or they're inert.
- Parallel Claude peers actively commit to nk3-deploy — coordinate via claude-peers before editing shared files (server.js).
- zsh gotchas: `for path in` wipes $PATH (use other var); unquoted $VAR doesn't word-split (use ${=VAR}).
- Mint NK3 token: docker exec tgroup-nk3-api node -e "require('jsonwebtoken').sign({employeeId,isCtv,lobScope,authLob},process.env.JWT_SECRET,...)".
