/**
 * IP Access Control types re-exported from /types/
 * @crossref:used-in[useIpAccessControl, IpAccessControl]
 */

import type { IpEntry, IpAccessSettings, IpAccessStats, IpAccessCheckResult, IpValidationResult, IpAccessMode } from '@/types/ipAccessControl';

export type { IpEntry, IpAccessSettings, IpAccessStats, IpAccessCheckResult, IpValidationResult, IpAccessMode };

export const MOCK_IP_ENTRIES: readonly IpEntry[] = [];
