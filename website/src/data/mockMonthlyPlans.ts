/**
 * Monthly Plans types re-exported from /types/
 * @crossref:used-in[Payment, hooks]
 */

import type { MonthlyPlan, Installment, InstallmentStatus, PlanStatus, PlanCreationInput } from '@/types/monthlyPlans';

export type { MonthlyPlan, Installment, InstallmentStatus, PlanStatus, PlanCreationInput };

export const MOCK_MONTHLY_PLANS: readonly MonthlyPlan[] = [];
