# Production / VPS build — used by `npx vite build` (NODE_ENV=production)
# VITE_API_URL is injected via docker-compose build arg, no hardcoded IP here.
# Fallback to relative /api which nginx can proxy.
VITE_API_URL=/api
VITE_GOOGLE_PLACES_API_KEY=AIzaSyDVk_KxoeAtvTa1-LvewB2OwrdnmZn-64c
