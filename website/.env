# Shared env — environment-specific values in .env.development / .env.production
# VITE_API_URL is set per environment (see .env.development, .env.production)
# Google API key is in .env.local (gitignored) for security
